<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>

  <body>
    <%- include("../partials/header.ejs") %>
    <main>
      <div>

        <article>
          <h3><%= "Animal(s):" %> <%= animal.species %></h3>
          <h3><%= "Location:"%> <%= animal.location %></h3>
          <h3><%= "Life Expectancy:"%> <%= animal.lifeExpectancy + " yrs"%></h3>
          <h3><%= "Extinct:"%> <%= animal.extinct %></h3>

          <img src="<%= animal.img %> " width="100" height="100" /> 


          <a href="/animals/<%= animal._id %>/edit"><button>Edit</button></a>
          <form action="/animals/<%= animal._id %>?_method=DELETE" method="POST">
            <input type="submit" value="Delete"/>
          </form>
          <a href="/animals/"><button>Back</button></a>
        </article>

      </div>
    </main>
  </body>
</html>